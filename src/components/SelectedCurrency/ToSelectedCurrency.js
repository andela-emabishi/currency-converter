import React from 'react';

const ToSelectedCurrency = (props) => {
  return (
    <div>
      ToSelectedCurrency
      {props.selectedToCurrency}
    </div>
  )
}

export default ToSelectedCurrency;
